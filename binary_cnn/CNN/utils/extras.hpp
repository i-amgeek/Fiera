#include "../tensor.hpp"

tensorg_2d convert_2d_float_to_gradient(tensor_2d input){


    int nx = in.shape()[0];
    int ny = in.shape()[1];
    // int h = .shape()[2];
    // int w = input.shape()[3];  
    tensorg_2d temp({nx,ny});

    for(int i=0; i<nx; i++){
        for(int j=0; j<ny; j++){
            tensorg_2d
        }
    } 
    temp

}